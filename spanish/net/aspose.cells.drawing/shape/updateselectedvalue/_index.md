---
title: UpdateSelectedValue
second_title: Referencia de API de Aspose.Cells para .NET
description: Actualiza el valor seleccionado por el valor de la celda vinculada.
type: docs
weight: 1170
url: /es/net/aspose.cells.drawing/shape/updateselectedvalue/
---
## Shape.UpdateSelectedValue method

Actualiza el valor seleccionado por el valor de la celda vinculada.

```csharp
public void UpdateSelectedValue()
```

### Ejemplos

```csharp

[C#]
//Inicializar un nuevo libro de trabajo.
//Libro de trabajo = nuevo libro de trabajo();

Cell cell = null;
for (int i = 0; i< 10; ++i)
{
    cell = book.Worksheets[0].Cells[i, 0];
    cell.Value = i + 1;
}
   
//Crear un objeto ListBox

//Controles ActiveX
//Aspose.Cells.Drawing.Shape listBox = book.Worksheets[0].Shapes.AddActiveXControl( Aspose.Cells.Drawing.ActiveXControls.ControlType.ListBox,2, 0, 2, 0, 130, 130);

//Controles de formulario
Aspose.Cells.Drawing.Shape listBox = book.Worksheets[0].Shapes.AddListBox(2, 0, 2, 0, 130, 130);

//Establece el rango usado para llenar el control.
listBox.SetInputRange("$A$1:$A$6", false, false);

//Establece el rango vinculado al valor del control.
listBox.SetLinkedCell("$A$12", false, true);

ListBox listbx = (ListBox)listBox;

//Establecer el valor de la celda A12
cell = book.Worksheets[0].Cells[11, 0];
cell.Value = 3;

//Actualiza el valor seleccionado por el valor de la celda vinculada.
listBox.UpdateSelectedValue();

//-1 predeterminado, ninguna opción seleccionada
if(listbx.IsSelected(2))
{
    //Se selecciona la opción 3 del ListBox
}

//Cambiar el valor de una celda vinculada
cell.Value = 4;

//Actualiza el valor seleccionado por el valor de la celda vinculada.
listBox.UpdateSelectedValue();
if(listbx.IsSelected(3))
{
    //Se selecciona la opción 4 del ListBox
}

```

### Ver también

* class [Shape](../../shape)
* espacio de nombres [Aspose.Cells.Drawing](../../shape)
* asamblea [Aspose.Cells](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Cells.dll -->
