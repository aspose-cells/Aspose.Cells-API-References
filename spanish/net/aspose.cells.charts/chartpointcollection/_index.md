---
title: ChartPointCollection
second_title: Referencia de API de Aspose.Cells para .NET
description: Representa una colección que contiene todos los puntos de una serie.
type: docs
weight: 520
url: /es/net/aspose.cells.charts/chartpointcollection/
---
## ChartPointCollection class

Representa una colección que contiene todos los puntos de una serie.

```csharp
public class ChartPointCollection
```

## Propiedades

| Nombre | Descripción |
| --- | --- |
| [Count](../../aspose.cells.charts/chartpointcollection/count) { get; } | Obtiene el recuento del punto del gráfico. |
| [Item](../../aspose.cells.charts/chartpointcollection/item) { get; } | Obtiene el[`ChartPoint`](../chartpoint) elemento en el índice especificado en la serie. |

## Métodos

| Nombre | Descripción |
| --- | --- |
| [Clear](../../aspose.cells.charts/chartpointcollection/clear)() | Eliminar toda la configuración de los puntos del gráfico. |
| [GetEnumerator](../../aspose.cells.charts/chartpointcollection/getenumerator)() | Devuelve un enumerador para todo el[`ChartPointCollection`](../chartpointcollection) . |
| [RemoveAt](../../aspose.cells.charts/chartpointcollection/removeat)(int) | Elimina punto en el índice de la serie.. |

### Ejemplos

```csharp
// Instanciando un objeto Workbook
Workbook workbook = new Workbook();

//Obteniendo la referencia de la primera hoja de cálculo
Worksheet worksheet = workbook.Worksheets[0];

// Agregar un valor de muestra a la celda "A1"
worksheet.Cells["A1"].PutValue(50);

// Agregar un valor de muestra a la celda "A2"
worksheet.Cells["A2"].PutValue(100);

// Agregar un valor de muestra a la celda "A3"
worksheet.Cells["A3"].PutValue(150);

// Agregar un valor de muestra a la celda "B1"
worksheet.Cells["B1"].PutValue(60);

// Agregar un valor de muestra a la celda "B2"
worksheet.Cells["B2"].PutValue(32);

// Agregar un valor de muestra a la celda "B3"
worksheet.Cells["B3"].PutValue(50);

//Añadiendo un gráfico a la hoja de trabajo
int chartIndex = worksheet.Charts.Add(ChartType.PieExploded, 5, 0, 25, 10);

//Accediendo a la instancia del gráfico recién agregado
Chart chart = worksheet.Charts[chartIndex];

// Agregar NSeries (fuente de datos del gráfico) al gráfico que va desde la celda "A1" hasta la celda "B3"
chart.NSeries.Add("A1:B3", true);

//Mostrar etiquetas de datos 
chart.NSeries[0].DataLabels.IsValueShown = true;

ChartPointCollection points = chart.NSeries[0].Points;

for (int i = 0; i < points.Count; i++)
{
    //Obtener punto de datos
    ChartPoint point = points[i];
    // Establecer explosión Pir
    point.Explosion = 15;
    //Establecer color de borde
    point.Border.Color = System.Drawing.Color.Red;
}

//Guardando el archivo de Excel
workbook.Save("book1.xls");

[VB.NET]

'Crear una instancia de un objeto Workbook
Dim workbook As Workbook = New Workbook()

'Obtención de la referencia de la primera hoja de cálculo
Dim worksheet As Worksheet = workbook.Worksheets(0)

'Adding a sample value to "A1" cell
worksheet.Cells("A1").PutValue(50)

'Adding a sample value to "A2" cell
worksheet.Cells("A2").PutValue(100)

'Adding a sample value to "A3" cell
worksheet.Cells("A3").PutValue(150)

'Adding a sample value to "B1" cell
worksheet.Cells("B1").PutValue(60)

'Adding a sample value to "B2" cell
worksheet.Cells("B2").PutValue(32)

'Adding a sample value to "B3" cell
worksheet.Cells("B3").PutValue(50)

'Adición de un gráfico a la hoja de cálculo
Dim chartIndex As Integer = worksheet.Charts.Add(ChartType.PieExploded, 5, 0, 25, 10)

'Acceso a la instancia del gráfico recién agregado
Dim chart As Chart = worksheet.Charts(chartIndex)

'Adding NSeries (chart data source) to the chart ranging from "A1" cell to "B3"
chart.NSeries.Add("A1:B3", True)

'Mostrar etiquetas de datos 
chart.NSeries(0).DataLabels.IsValueShown = True

Dim points As ChartPointCollection = chart.NSeries(0).Points

For i As Integer = 0 To points.Count - 1
    'Obtener punto de datos
    Dim point As ChartPoint = points(i)
    'Establecer explosión Pir
    point.Explosion = 15
    'Establecer color de borde
    point.Border.Color = System.Drawing.Color.Red
Next i

'Guardar el archivo de Excel
workbook.Save("book1.xls")

```

### Ver también

* espacio de nombres [Aspose.Cells.Charts](../../aspose.cells.charts)
* asamblea [Aspose.Cells](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Cells.dll -->
