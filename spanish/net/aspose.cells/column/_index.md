---
title: Column
second_title: Referencia de API de Aspose.Cells para .NET
description: Representa una sola columna en una hoja de cálculo.
type: docs
weight: 1060
url: /es/net/aspose.cells/column/
---
## Column class

Representa una sola columna en una hoja de cálculo.

```csharp
public class Column
```

## Propiedades

| Nombre | Descripción |
| --- | --- |
| [GroupLevel](../../aspose.cells/column/grouplevel) { get; } | Obtiene el nivel de grupo de la columna. |
| [Index](../../aspose.cells/column/index) { get; } | Obtiene el índice de esta columna. |
| [IsCollapsed](../../aspose.cells/column/iscollapsed) { get; set; } | si la columna está colapsada |
| [IsHidden](../../aspose.cells/column/ishidden) { get; set; } | Indica si la columna está oculta. |
| [Style](../../aspose.cells/column/style) { get; } | Obtiene el estilo de esta columna. |
| [Width](../../aspose.cells/column/width) { get; set; } | Obtiene y establece el ancho de columna en unidades de caracteres. |

## Métodos

| Nombre | Descripción |
| --- | --- |
| [ApplyStyle](../../aspose.cells/column/applystyle)(Style, StyleFlag) | Aplica formatos para una columna completa. |

### Ejemplos

```csharp

[C#]

// Instanciando un objeto Workbook
Workbook workbook = new Workbook();

//Obteniendo la referencia de la primera hoja de cálculo
Worksheet worksheet = workbook.Worksheets[0];
Style style = workbook.CreateStyle();

//Configurando el color de fondo a Azul
style.BackgroundColor = Color.Blue;

//Establecer el color de primer plano en rojo
style.ForegroundColor= Color.Red;

//estableciendo el patrón de fondo
style.Pattern = BackgroundType.DiagonalStripe;

//Bandera de nuevo estilo
StyleFlag styleFlag = new StyleFlag();

//Establecer todos los estilos
styleFlag.All = true;

//Obtener la primera columna
Column column = worksheet.Cells.Columns[0];

//Aplicar estilo a la primera columna
column.ApplyStyle(style, styleFlag);

//Guardando el archivo de Excel
workbook.Save("book1.xls");

[VB.NET]

'Crear una instancia de un objeto Workbook
Dim workbook As Workbook = New Workbook()

'Obtención de la referencia de la primera hoja de cálculo
Dim worksheet As Worksheet = workbook.Worksheets(0)

Dim style As Style = workbook.CreateStyle()

'Establecer el color de fondo en Azul
style.BackgroundColor = Color.Blue

'Establecer el color de primer plano en rojo
style.ForegroundColor = Color.Red

'Configuración del patrón de fondo
style.Pattern = BackgroundType.DiagonalStripe

'Bandera de nuevo estilo
Dim styleFlag As New StyleFlag()

'Establecer todos los estilos
styleFlag.All = True

'Obtener la primera columna
Dim column As Column = worksheet.Cells.Columns(0)

'Aplicar estilo a la primera columna
column.ApplyStyle(style, styleFlag)

'Guardar el archivo de Excel
workbook.Save("book1.xls")
```

### Ver también

* espacio de nombres [Aspose.Cells](../../aspose.cells)
* asamblea [Aspose.Cells](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Cells.dll -->
