---
title: "CellsColor"
linktitle: "CellsColor"
second_title: "Aspose.Cells for Node.js via Java API Reference"
description: "Represents all types of color."
type: docs
weight: 410
url: /nodejs/aspose.cells/cellscolor/
---

## CellsColor class

Represents all types of color.

## Methods

| Name | Description |
| --- | --- |
| [getArgb()](#getargb) | Gets and sets the color from a 32-bit ARGB value. |
| [getColor()](#getcolor) | Gets and sets the RGB color. |
| [getColorIndex()](#getcolorindex) | Gets and sets the color index in the color palette. Only applies of indexed color. |
| [getThemeColor()](#getthemecolor) | Gets the theme color. Only applies for theme color type. |
| [getTransparency()](#gettransparency) | Gets and sets transparency as a value from 0.0 (opaque) through 1.0 (clear). |
| [getType()](#gettype) | The color type. The value of the property is ColorType integer constant. |
| [isShapeColor()](#isshapecolor) | Gets and set the color which should apply to cell or shape. The expression of the color of the cell and the shape is dif |
| [setArgb()](#setargb) | Gets and sets the color from a 32-bit ARGB value. |
| [setColor()](#setcolor) | Gets and sets the RGB color. |
| [setColorIndex()](#setcolorindex) | Gets and sets the color index in the color palette. Only applies of indexed color. |
| [setShapeColor()](#setshapecolor) | Gets and set the color which should apply to cell or shape. The expression of the color of the cell and the shape is dif |
| [setThemeColor()](#setthemecolor) | Gets the theme color. Only applies for theme color type. |
| [setTintOfShapeColor(tint)](#settintofshapecolor) | Set the tint of the shape color |
| [setTransparency()](#settransparency) | Gets and sets transparency as a value from 0.0 (opaque) through 1.0 (clear). |

### getArgb() {#getargb}

Gets and sets the color from a 32-bit ARGB value.

### getColor() {#getcolor}

Gets and sets the RGB color.

### getColorIndex() {#getcolorindex}

Gets and sets the color index in the color palette. Only applies of indexed color.

### getThemeColor() {#getthemecolor}

Gets the theme color. Only applies for theme color type.

### getTransparency() {#gettransparency}

Gets and sets transparency as a value from 0.0 (opaque) through 1.0 (clear).

### getType() {#gettype}

The color type. The value of the property is ColorType integer constant.

### isShapeColor() {#isshapecolor}

Gets and set the color which should apply to cell or shape. The expression of the color of the cell and the shape is different. For example: the theme color with same tint value will be not same in the cell and the shape.

### setArgb() {#setargb}

Gets and sets the color from a 32-bit ARGB value.

### setColor() {#setcolor}

Gets and sets the RGB color.

### setColorIndex() {#setcolorindex}

Gets and sets the color index in the color palette. Only applies of indexed color.

### setShapeColor() {#setshapecolor}

Gets and set the color which should apply to cell or shape. The expression of the color of the cell and the shape is different. For example: the theme color with same tint value will be not same in the cell and the shape.

### setThemeColor() {#setthemecolor}

Gets the theme color. Only applies for theme color type.

### setTintOfShapeColor(tint) {#settintofshapecolor}

Set the tint of the shape color

| Parameter | Type | Description |
| --- | --- | --- |
| tint | Number |  |

### setTransparency() {#settransparency}

Gets and sets transparency as a value from 0.0 (opaque) through 1.0 (clear).
