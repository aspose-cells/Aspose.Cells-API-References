---
title: LoadFilter
second_title: Aspose.Cells لمرجع .NET API
description: يمثل عامل التصفية الذي يوفر خيارات لتحميل البيانات عند تحميل المصنف من القالب.
type: docs
weight: 3980
url: /ar/net/aspose.cells/loadfilter/
---
## LoadFilter class

يمثل عامل التصفية الذي يوفر خيارات لتحميل البيانات عند تحميل المصنف من القالب.

```csharp
public class LoadFilter
```

## المنشئون

| اسم | وصف |
| --- | --- |
| [LoadFilter](loadfilter#constructor)() | ينشئ LoadFilter واحدًا بخيارات التصفية الافتراضية LoadDataFilterOptions.All. |
| [LoadFilter](loadfilter#constructor_1)(LoadDataFilterOptions) | ينشئ LoadFilter واحدًا بخيارات مرشح معينة. |

## الخصائص

| اسم | وصف |
| --- | --- |
| [LoadDataFilterOptions](../../aspose.cells/loadfilter/loaddatafilteroptions) { get; set; } | خيارات التصفية للإشارة إلى البيانات التي يجب تحميلها. |
| virtual [SheetsInLoadingOrder](../../aspose.cells/loadfilter/sheetsinloadingorder) { get; } | تحدد الأوراق (الفهارس) وترتيب تحميلها. القيمة الافتراضية خالية ، وهذا يدل على تحميل كافة الأوراق بالترتيب الافتراضي في ملف القالب. لن يتم تحميلها. |

## طُرق

| اسم | وصف |
| --- | --- |
| virtual [StartSheet](../../aspose.cells/loadfilter/startsheet)(Worksheet) | إعداد خيارات التصفية قبل تحميل ورقة العمل المحددة. يمكن أن يؤدي تنفيذ المستخدم لـ LoadFilter إلى تغيير LoadDataFilterOptions هنا للإشارة إلى كيفية تحميل البيانات لورقة العمل هذه. |

### ملاحظات

يمكن للمستخدم تحديد خيارات التصفية أو تنفيذ LoadFilter الخاص به لتحديد كيفية تحميل البيانات.

### أمثلة

يوضح المثال التالي كيفية تحديد خيارات التصفية وفقًا لخصائص ورقة العمل.

```csharp
[C#]
Workbook wb = new Workbook(template, new LoadOptions() { LoadFilter = new LoadFilterSheet() });
// تنفيذ LoadFilter المخصص
class LoadFilterSheet : LoadFilter
{
    public override void StartSheet(Worksheet sheet) 
    {
        if (sheet.Name == "Sheet1")
        {
            LoadDataFilterOptions = Aspose.Cells.LoadDataFilterOptions.All;
        }
        else
        {
            LoadDataFilterOptions = Aspose.Cells.LoadDataFilterOptions.None;
        }
    }
}
```

### أنظر أيضا

* مساحة الاسم [Aspose.Cells](../../aspose.cells)
* المجسم [Aspose.Cells](../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Cells.dll -->
