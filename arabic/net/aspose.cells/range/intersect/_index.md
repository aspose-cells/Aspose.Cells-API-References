---
title: Intersect
second_title: Aspose.Cells لمرجع .NET API
description: إرجاع أRangeaspose.cells/range يمثل التقاطع المستطيل لنطاقين.
type: docs
weight: 320
url: /ar/net/aspose.cells/range/intersect/
---
## Range.Intersect method

إرجاع أ[`Range`](../../range) يمثل التقاطع المستطيل لنطاقين.

```csharp
public Range Intersect(Range range)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| range | Range | النطاق المتقاطع. |

### قيمة الإرجاع

يعود أ[`Range`](../../range) هدف

### ملاحظات

إذا لم يتم تقاطع النطاقين ، يتم إرجاع قيمة خالية.

### أمثلة

```csharp

[C#]

// إنشاء كائن مصنف
Workbook workbook = new Workbook();
// احصل على خلايا ورقة العمل الأولى.
Cells cells = workbook.Worksheets[0].Cells;
Range range1 = cells.CreateRange("A1:A5");
Range range2 = cells.CreateRange("A3:A10");
// احصل على نطاق متقاطع من النطاقين.
Range intersectRange = range1.Intersect(range2);
// احفظ ملف Excel
workbook.Save("book1.xlsm");

 [Visual Basic]

'إنشاء كائن مصنف
Dim workbook As Workbook = New Workbook()
'احصل على خلايا ورقة العمل الأولى.
Dim cells as Cells = workbook.Worksheets[0].Cells
Range range1 = cells.CreateRange("A1:A5")
Range range2 = cells.CreateRange("A3:A10")
'الحصول على نطاق متقاطع من النطاقين.
Range intersectRange = range1.Intersect(range2)
'احفظ ملف Excel
workbook.Save("book1.xlsm")
```

### أنظر أيضا

* class [Range](../../range)
* مساحة الاسم [Aspose.Cells](../../range)
* المجسم [Aspose.Cells](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Cells.dll -->
