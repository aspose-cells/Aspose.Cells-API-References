---
title: SetInputRange
second_title: Aspose.Cells لمرجع .NET API
description: يحدد النطاق المستخدم لملء عنصر التحكم.
type: docs
weight: 1120
url: /ar/net/aspose.cells.drawing/shape/setinputrange/
---
## Shape.SetInputRange method

يحدد النطاق المستخدم لملء عنصر التحكم.

```csharp
public void SetInputRange(string formula, bool isR1C1, bool isLocal)
```

| معامل | يكتب | وصف |
| --- | --- | --- |
| formula | String | النطاق المستخدم لملء عنصر التحكم. |
| isR1C1 | Boolean | ما إذا كانت الصيغة تحتاج إلى تنسيقها كـ R1C1. |
| isLocal | Boolean | ما إذا كانت الصيغة تحتاج إلى التنسيق بواسطة الإعدادات المحلية. |

### أمثلة

```csharp

[C#]
// بعد تنفيذ الكود أدناه ، يتم إنشاء كائن ListBox في الملف الذي تم إنشاؤه. عند النقر فوق الخيار المحدد ، يتم عرض القيمة المحددة في الخلية A12.

// تهيئة مصنف جديد.
// كتاب المصنف = مصنف جديد () ;

for (int i = 0; i< 10; ++i)
{
    Cell cell = book.Worksheets[0].Cells[i, 0];
    cell.Value = i + 1;
}
   
// إنشاء كائن ListBox

// عناصر تحكم ActiveX
//Aspose.Cells.Drawing.Shape listBox = book.Worksheets [0] .Shapes.AddActiveXControl (Aspose.Cells.Drawing.ActiveXControls.ControlType.ListBox، 2، 0، 2، 0، 130، 130) ;

// ضوابط النموذج
Aspose.Cells.Drawing.Shape listBox = book.Worksheets[0].Shapes.AddListBox(2, 0, 2, 0, 130, 130);

// يعين النطاق المستخدم لملء عنصر التحكم.
listBox.SetInputRange("$A$1:$A$6", false, false);

// يعين النطاق المرتبط بقيمة عنصر التحكم.
listBox.SetLinkedCell("$A$12", false, true);

```

### أنظر أيضا

* class [Shape](../../shape)
* مساحة الاسم [Aspose.Cells.Drawing](../../shape)
* المجسم [Aspose.Cells](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Cells.dll -->
