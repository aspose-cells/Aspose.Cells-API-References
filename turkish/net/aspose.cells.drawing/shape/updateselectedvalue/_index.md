---
title: UpdateSelectedValue
second_title: Aspose.Cells for .NET API Referansı
description: Seçilen değeri bağlantılı hücrenin değerine göre güncelleyin.
type: docs
weight: 1170
url: /tr/net/aspose.cells.drawing/shape/updateselectedvalue/
---
## Shape.UpdateSelectedValue method

Seçilen değeri bağlantılı hücrenin değerine göre güncelleyin.

```csharp
public void UpdateSelectedValue()
```

### Örnekler

```csharp

[C#]
//Yeni bir çalışma kitabı başlat.
//Çalışma kitabı kitabı = yeni Çalışma Kitabı();

Cell cell = null;
for (int i = 0; i< 10; ++i)
{
    cell = book.Worksheets[0].Cells[i, 0];
    cell.Value = i + 1;
}
   
//ListBox nesnesi oluştur

//ActiveX Kontrolleri
//Aspose.Cells.Drawing.Shape listBox = book.Worksheets[0].Shapes.AddActiveXControl( Aspose.Cells.Drawing.ActiveXControls.ControlType.ListBox,2, 0, 2, 0, 130, 130);

//Form Kontrolleri
Aspose.Cells.Drawing.Shape listBox = book.Worksheets[0].Shapes.AddListBox(2, 0, 2, 0, 130, 130);

//Kontrolü doldurmak için kullanılan aralığı ayarlar.
listBox.SetInputRange("$A$1:$A$6", false, false);

//Kontrolün değerine bağlı aralığı ayarlar.
listBox.SetLinkedCell("$A$12", false, true);

ListBox listbx = (ListBox)listBox;

// A12 hücresinin değerini ayarla
cell = book.Worksheets[0].Cells[11, 0];
cell.Value = 3;

//Bağlı hücrenin değerine göre seçili değeri güncelle.
listBox.UpdateSelectedValue();

//-1 varsayılan, hiçbir seçenek seçilmedi
if(listbx.IsSelected(2))
{
    //ListBox'ın 3. seçeneği seçili
}

//Bağlı bir hücrenin değerini değiştir
cell.Value = 4;

//Bağlı hücrenin değerine göre seçili değeri güncelle.
listBox.UpdateSelectedValue();
if(listbx.IsSelected(3))
{
    //ListBox'ın 4. seçeneği seçili
}

```

### Ayrıca bakınız

* class [Shape](../../shape)
* ad alanı [Aspose.Cells.Drawing](../../shape)
* toplantı [Aspose.Cells](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Cells.dll -->
