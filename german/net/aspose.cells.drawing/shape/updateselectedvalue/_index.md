---
title: UpdateSelectedValue
second_title: Aspose.Cells für .NET-API-Referenz
description: Aktualisiert den ausgewählten Wert um den Wert der verknüpften Zelle.
type: docs
weight: 1170
url: /de/net/aspose.cells.drawing/shape/updateselectedvalue/
---
## Shape.UpdateSelectedValue method

Aktualisiert den ausgewählten Wert um den Wert der verknüpften Zelle.

```csharp
public void UpdateSelectedValue()
```

### Beispiele

```csharp

[C#]
//Eine neue Arbeitsmappe initialisieren.
//Arbeitsmappenbuch = neue Arbeitsmappe();

Cell cell = null;
for (int i = 0; i< 10; ++i)
{
    cell = book.Worksheets[0].Cells[i, 0];
    cell.Value = i + 1;
}
   
//Ein ListBox-Objekt erstellen

//ActiveX-Steuerelemente
//Aspose.Cells.Drawing.Shape listBox = book.Worksheets[0].Shapes.AddActiveXControl( Aspose.Cells.Drawing.ActiveXControls.ControlType.ListBox,2, 0, 2, 0, 130, 130);

//Formularsteuerelemente
Aspose.Cells.Drawing.Shape listBox = book.Worksheets[0].Shapes.AddListBox(2, 0, 2, 0, 130, 130);

//Legt den Bereich fest, der zum Füllen des Steuerelements verwendet wird.
listBox.SetInputRange("$A$1:$A$6", false, false);

//Legt den Bereich fest, der mit dem Wert des Steuerelements verknüpft ist.
listBox.SetLinkedCell("$A$12", false, true);

ListBox listbx = (ListBox)listBox;

//Setzen Sie den Wert von Zelle A12
cell = book.Worksheets[0].Cells[11, 0];
cell.Value = 3;

//Aktualisiert den ausgewählten Wert um den Wert der verknüpften Zelle.
listBox.UpdateSelectedValue();

//-1 Standard, keine Option ausgewählt
if(listbx.IsSelected(2))
{
    //Option 3 der ListBox ist ausgewählt
}

//Wert einer verknüpften Zelle ändern
cell.Value = 4;

//Aktualisiert den ausgewählten Wert um den Wert der verknüpften Zelle.
listBox.UpdateSelectedValue();
if(listbx.IsSelected(3))
{
    //Option 4 der ListBox ist ausgewählt
}

```

### Siehe auch

* class [Shape](../../shape)
* namensraum [Aspose.Cells.Drawing](../../shape)
* Montage [Aspose.Cells](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Cells.dll -->
