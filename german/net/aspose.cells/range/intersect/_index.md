---
title: Intersect
second_title: Aspose.Cells für .NET-API-Referenz
description: Gibt a zurückRangeaspose.cells/range Objekt das den rechteckigen Schnittpunkt zweier Bereiche darstellt.
type: docs
weight: 320
url: /de/net/aspose.cells/range/intersect/
---
## Range.Intersect method

Gibt a zurück[`Range`](../../range) Objekt, das den rechteckigen Schnittpunkt zweier Bereiche darstellt.

```csharp
public Range Intersect(Range range)
```

| Parameter | Typ | Beschreibung |
| --- | --- | --- |
| range | Range | Der Schnittbereich. |

### Rückgabewert

Gibt ein[`Range`](../../range) Objekt

### Bemerkungen

Wenn sich die beiden Bereiche nicht schneiden, wird null zurückgegeben.

### Beispiele

```csharp

[C#]

//Instanziieren eines Workbook-Objekts
Workbook workbook = new Workbook();
// Holen Sie sich die ersten Arbeitsblattzellen.
Cells cells = workbook.Worksheets[0].Cells;
Range range1 = cells.CreateRange("A1:A5");
Range range2 = cells.CreateRange("A3:A10");
//Ermittle den Schnittbereich der beiden Bereiche.
Range intersectRange = range1.Intersect(range2);
//Excel-Datei speichern
workbook.Save("book1.xlsm");

 [Visual Basic]

'Instanziieren eines Workbook-Objekts
Dim workbook As Workbook = New Workbook()
'Holen Sie sich die ersten Arbeitsblattzellen.
Dim cells as Cells = workbook.Worksheets[0].Cells
Range range1 = cells.CreateRange("A1:A5")
Range range2 = cells.CreateRange("A3:A10")
'Holen Sie sich den Schnittbereich der beiden Bereiche.
Range intersectRange = range1.Intersect(range2)
'Speichern Sie die Excel-Datei
workbook.Save("book1.xlsm")
```

### Siehe auch

* class [Range](../../range)
* namensraum [Aspose.Cells](../../range)
* Montage [Aspose.Cells](../../../)

<!-- DO NOT EDIT: generated by xmldocmd for Aspose.Cells.dll -->
